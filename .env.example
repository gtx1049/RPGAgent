# .env.example - 环境变量配置示例
# 复制为 .env 并填入实际值：
#   cp .env.example .env

# OpenAI API 密钥（必填）
OPENAI_API_KEY=sk-your-key-here

# API 地址（可选，默认 https://api.openai.com/v1）
OPENAI_BASE_URL=https://api.openai.com/v1

# 模型名称（可选，默认 MiniMax-M2.7）
# 也支持旧变量名 RPG_MODEL（向后兼容）
OPENAI_MODEL=gpt-4-turbo
